<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
	xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
    prism:ViewModelLocator.AutowireViewModel="True"
	xmlns:local="clr-namespace:DevonApp3.Views;assembly=DevonApp3"
    x:Class="DevonApp3.Views.Analytics"
    xmlns:oxy="clr-namespace:OxyPlot.Xamarin.Forms;assembly=OxyPlot.Xamarin.Forms"
    BackgroundImage="simulation.png"
    Title="Analytics">



	  <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
        <Image Source="bg">
        </Image>
        <!-- Label x:Name="strvaluepoint" LineBreakMode="CharacterWrap" FontSize="Small" HorizontalOptions="Center"/ -->
        <!-- Label x:Name="strtimestamp" LineBreakMode="CharacterWrap" FontSize="Small" HorizontalOptions="Center"/ -->
        <!-- ListView ItemsSource="{Binding Path=result.Value;}" / -->
        <!-- oxy:PlotView  Model="{Binding PlotModel}" VerticalOptions="Center" HorizontalOptions="Center" / -->
    </StackLayout>
    

</ContentPage>

